package app.handlive.android.core.design.component

/** Hai dạng của chỉ báo. */
enum class HLStatusIndicatorVariant {
    /** Chấm màu + chữ `secondary-label`, dùng trong dòng và đầu màn. */
    Inline,

    /** Viên `tertiary-system-fill`, chữ `label`, nhãn ngắn ("LAN"); cho đầu popover, cửa sổ. */
    Pill,
}
